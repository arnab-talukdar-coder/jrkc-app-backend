/**
 * Mock Data Seeder (Disabled)
 * No automatic data seeding or modifications performed on startup or deployment.
 */

export async function seedDevelopmentData() {
  // Data seeder disabled as requested
}
